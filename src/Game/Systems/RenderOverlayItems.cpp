#include <glm/gtc/matrix_transform.hpp>
#include "../Config.hpp"

#include "RenderOverlayItems.hpp"

namespace Systems
{
	void renderOverlayItems(const Core::ResourceCache &cache, entt::registry &registry)
	{
		//


		//auto program = cache.shader_programs.handle("overlay"_hs);
		//program->useProgram();

		//float window_width = display_config.size.width;
		//float window_height = display_config.size.width;
		//glm::mat4 ortho = glm::ortho(0.0f, window.width, 0.0f, window.height);

		// pass params to program

		// draw
	}
}